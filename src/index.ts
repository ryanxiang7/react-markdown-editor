import Editor, { getEditorConfig, getMDVal, getHTMLVal, use, unuse } from './editor';

import { BTNTYPE } from './utils/var';

export default Editor;

export { getEditorConfig, getHTMLVal, getMDVal, use, unuse, BTNTYPE };
